const PROJECT = 'project';
const ADMIN_NAME = 'admin';

export const STACK = `${PROJECT.charAt(0).toUpperCase() + PROJECT.slice(1)}${ADMIN_NAME.charAt(0).toUpperCase() + ADMIN_NAME.slice(1)}`;
export const PREFIX = `${PROJECT}_${ADMIN_NAME}`;
